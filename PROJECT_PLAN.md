# 远场磁层磁场-电场快速代理模型 项目企划书

> 基于 SWMF/BATSRUS 的 90 Re 磁层输出，训练"物理形式 + 神经网络"混合代理模型，用于磁场/电场的快速、可迁移仿真。

---

## 1. 项目概述

### 1.1 背景与问题

- 现有经验模型（Tsyganenko T96/T04）是为**内磁层**（< ~20 Re）设计的，在磁层顶外、磁尾远场、磁鞘、弓激波区域**失效**。
- 需要覆盖 **90 Re** 范围、且**随太阳风条件变化**的磁场 B 与电场 E 的快速模型。
- 目标应用为**粒子追踪等**，因此 B、E 及其空间梯度 `∇B` 都必需。

### 1.2 核心方案（已确定）

采用**物理势函数表示 + 神经网络拟合系数**的混合架构：

```
B = ∇ × A            （磁矢势，保证 ∇·B = 0）
E = -u × B           （欧姆定律，预案 A，已确认数据含速度场）
```

- 势函数 `A`（及速度 `u`）用物理基函数展开，基函数系数由 NN 根据太阳风条件预测。
- NN 只拟合"太阳风 → 系数"的映射，不直接输出场量，从而兼顾物理可解释性与拟合精度。

### 1.3 输入输出定义

| | 内容 |
|---|---|
| 输入 | 空间坐标 `(x, y, z)` + 太阳风条件（IMF `Bx/By/Bz`、速度、密度/动压、温度等） |
| 输出 | 磁场 `B = ∇×A`、电场 `E = -u×B`、速度 `u`、梯度 `∇B` |

---

## 2. 目标与验收标准（量化）

> 本节为企划书补充的**空缺**：此前讨论未定义"怎样算成功"。

### 2.1 功能目标

1. 输入任意 90 Re 内的空间点 + 太阳风条件，输出 B、E、u 及 `∇B`。
2. 内场（< 15 Re）与 T 系列模型连续性衔接。
3. 推理可在**纯 CPU**（脱离 PyTorch/CUDA）上运行。

### 2.2 精度目标（初步，需跑数据后标定）

| 指标 | 目标 |
|---|---|
| 磁场相对误差 `\|B_pred - B_true\| / \|B_true\|` | 内场 < 5%，远场 < 15% |
| 磁场方向误差（夹角） | 内场 < 3°，远场 < 10° |
| 无散度残差 `∇·B / \|B\|` | < 1e-3（势函数下应 ≈ 0） |
| 电场相对误差 | 待标定（< 20% 起步） |

### 2.3 性能目标

- 单点推理：**微秒级**（CPU）。
- 百万点批量推理：**秒级**（CPU）。

### 2.4 验收标准（SMART）

- [ ] 在**训练集之外的太阳风条件**下，磁场相对误差满足 2.2 阈值。
- [ ] `∇·B` 残差在物理许可范围内（~0）。
- [ ] 磁力线追踪结果与 SWMF 原始输出对比，关键拓扑结构一致。
- [ ] 导出 ONNX / 纯公式后，CPU 推理结果与训练框架一致（误差 < 1e-6）。

---

## 3. 技术方案

### 3.1 核心架构（物理势函数 + NN）

```
模型输出: A(x,y,z; SW)  和  u(x,y,z; SW)
B = ∇ × A               （自动微分）
E = -u × B              （自动微分叉积）
∇B = ∇(∇×A)             （二阶自动微分，供梯度/曲率漂移）
```

- **无散度保证**：`B = ∇×A` 数学恒等，`∇·B ≡ 0`，无需惩罚项。
- **电场自洽**：`E = -u×B` 由欧姆定律保证 B/E 物理一致。
- **粒子追踪红利**：`∇B`、曲率等由自动微分解析得到，无需数值差分。

### 3.2 预案 A（主方案，已确认可行）

数据含速度场（代码已确认 `ux/uy/uz`、`Ex/Ey/Ez` 存在）。

- 输出头：`A`(3) + `u`(3)，共 6 分量。
- `B = ∇×A`，`E = -u×B`。
- 优点：物理最完整，兼容非稳态（若后续用时间序列数据）。

### 3.3 预案 B（备用，若 Ex/Ey/Ez 未写入输出）

数据无速度场时，电场用稳态电势表示：

```
B = ∇ × A
E = -∇ Φ                （电势，稳态下 ∇×E = 0）
```

- 输出头：`A`(3) + `Φ`(1)，共 4 分量。
- 风险：隐含"稳态无旋电场"假设，磁暴时相/感应电场会漏项。

### 3.4 基函数选型（远场物理分区）

> 本节为企划书补充的**空缺**：此前未落到具体基函数。

| 区域 | 大致范围 | 物理基函数 |
|---|---|---|
| 内磁层 | < 15–20 Re | 偶极 + 环电流（复用 T 系列内场形式） |
| 磁尾 | 20–60 Re | Harris 电流片型解 |
| 磁层顶过渡 | 边界层 | Shue 型磁层顶参数化 + NN 平滑 |
| 磁鞘 | 磁层顶↔弓激波 | 压缩/偏转 IMF |
| 弓激波外 | > 弓激波 | 未扰动 IMF + 太阳风参数 |

- 磁层顶/弓激波位置是**动压 Pdyn 的函数**，需显式参数化（这是远场模型成败关键）。
- 各区域用平滑权重函数拼接（类 T 系列的 fade-out 函数）。

### 3.5 规范自由度处理（磁矢势 A 的 gauge）

> 本节为企划书补充的**空缺**：A 的规范不唯一会导致训练发散。

- 同一个 B 对应无穷多 A（`A → A + ∇χ`），需固定规范。
- 方案：在损失中加**库仑规范正则项** `λ · (∇·A)²`，或固定某一种规范。
- 这是"全 NN 输出 A"方案的核心难点；若用物理基函数展开，gauge 由基函数天然锁定。

---

## 4. 数据策略

### 4.1 数据来源

- SWMF/BATSRUS 的 90 Re 全球磁层稳态解（GM 组件）。
- 多太阳风条件扫描（见 4.2）。

### 4.2 太阳风参数空间采样（DOE）

> 本节为企划书补充的**空缺**：参数空间如何覆盖。

| 参数 | 采样范围（建议） |
|---|---|
| IMF Bz | -10 ~ +10 nT（决定磁层顶/磁尾形态，重点扫） |
| IMF By | -10 ~ +10 nT |
| 太阳风速度 | 300 ~ 800 km/s |
| 密度 / 动压 Pdyn | 0.5 ~ 10 nPa |

- 建议**拉丁超立方采样**或正交设计，覆盖参数空间边缘。
- 初始可先做 ~20 个条件，再根据外推表现加密。

### 4.3 稳态 vs 时间序列

- 阶段 1 用**稳态快照**（每个条件一个解），满足 `∇×E = 0`。
- 后续如需磁暴时相，再引入时间序列（此时预案 A 的 `E=-u×B` 自然兼容感应电场）。

### 4.4 数据提取管线

- 用 SpacePy / `share/Python/Scripts/` 读 BATSRUS 输出。
- 提取字段：`x, y, z, rho, ux, uy, uz, bx, by, bz`（+ `ex, ey, ez` 若存在）+ 太阳风标签。
- 输出为 float32 numpy / HDF5。

### 4.5 采样策略（间断处加密）

> 本节为企划书补充的**空缺**：磁层顶/弓激波是间断，拟合精度取决于数据密度。

- 在磁层顶、弓激波附近**加密采样**。
- 训练时对间断区域样本加权（间断感知损失）。

### 4.6 训练 / 验证 / 测试划分

> 本节为企划书补充的**空缺**：此前未谈划分与防泄漏。

| 集合 | 划分方式 | 用途 |
|---|---|---|
| 训练集 | 随机 70%（按空间点） | 拟合 |
| 验证集 | 随机 15% | 超参调优 |
| 测试集 | 随机 15% + **留出整组太阳风条件** | 评估 + **外推能力** |

- **关键**：外推测试必须用**训练时完全没见过的太阳风条件**，避免"内插假象"。

### 4.7 坐标系与单位制

> 本节为企划书补充的**空缺**：坐标与单位是实际数据处理的坑。

| 项 | 建议 |
|---|---|
| 坐标系 | 远场（太阳风主导）用 **GSE**；内场（偶极主导）用 **GSM**；训练时统一到单一坐标系并记录变换 |
| 长度单位 | 统一为地球半径 **Re**（1 Re = 6371 km），与 T 系列一致 |
| 磁场单位 | nT（与太阳风 IMF 量级一致） |
| 归一化 | NN 输入输出做标准化（z-score 或物理量纲缩放），避免量纲差异导致训练失衡 |

- 坐标变换（GSE↔GSM）需随偶极倾角/太阳风方向处理，建议在预处理阶段固化并落盘。

### 4.8 物理对称性数据增强

> 本节为企划书补充的**空缺**：可利用磁层对称性扩充有效数据。

- **南北镜像**：IMF By 反号时，磁场北向分量反号（By 对称性）。
- **偶极轴对齐**：对偶极倾角做旋转变换扩充样本。
- 对称性增强可有效扩充训练集、改善外推，但**必须严格保持物理约束一致**（增强后仍满足 ∇·B=0）。

---

## 5. 模型与训练

### 5.1 NN 结构

- 输入：`(x,y,z,SW)` ≈ 9–11 维。
- 输出：`A`(3) + `u`(3)（预案 A）。
- 结构：MLP 6–8 层 × 256 宽（坐标网络/隐式场表示）。
- 参数量约 40 万。

### 5.2 损失函数

```
L = L_fit + λ_div · L_gauge
L_fit   = |B_pred - B_true|² + |E_pred - E_true|² + |u_pred - u_true|²
L_gauge = |∇·A|²            （库仑规范正则）
```

### 5.3 训练硬件

- 模型小（40 万参数），显存需求 < 2 GB。
- **CPU（EPYC 9845）训练可行**，且因数据全驻留 128 GB RAM、省去 host→device 拷贝，可能不比 GPU 慢。
- 优先 CPU 后端跑 benchmark，实测后再决定是否用 RTX 5080。

### 5.4 实验可复现性

> 本节为企划书补充的**空缺**：训练需可追溯、可复现。

- 固定随机种子（数据划分、权重初始化、数据增强）。
- 记录数据版本（SWMF 输出 commit、预处理脚本哈希）。
- 保存训练配置（超参、损失权重 λ）与 checkpoint。
- 使用 ML 实验追踪（如 MLflow / wandb 或简化为 JSON 日志）。

---

## 6. 评估与验证

### 6.1 评估指标

- 拟合误差：B/E 相对误差、方向误差（见 2.2）。
- 物理残差：`∇·B`、`∇×E`。
- 端到端：磁力线追踪、粒子轨迹对比 SWMF 原始输出。

### 6.2 基准对比（baseline）

> 本节为企划书补充的**空缺**：需要可对比的 baseline。

1. **Tsyganenko T96/T04**（SWMF 内置）：内场基准，验证新模型在内场退化为 T 系列。
2. **SWMF 原始输出**：ground truth。

### 6.3 外推测试

- 用留出的太阳风条件，检验模型在训练参数空间外的表现。
- 重点看磁层顶/弓激波位置随 Pdyn 的变化是否正确。

---

## 7. 部署与可迁移性

> 本节为企划书补充的**空缺**：交付形态此前未明确。

| 交付形态 | 说明 |
|---|---|
| ONNX | GPU 训练 → 导出 ONNX → CPU 推理，脱离 PyTorch/CUDA |
| 纯解析式 + 系数 | 若走"物理基函数 + 系数"，最终是解析公式 + 查表，最大可迁移 |
| 纯 C/Fortran | 前向传播手写或代码生成，可嵌入粒子追踪代码 |

- 目标：部署在**任意 CPU**，不依赖 NVIDIA。

---

## 8. 里程碑规划

| 阶段 | 内容 | 交付物 | GPU 参与 |
|---|---|---|---|
| M0 | 确认输出格式与速度/电场变量 | 数据样例 + 读取脚本 | 否 |
| M1 | 单太阳风条件、全 NN（输出 A）原型 | 验证远场能否被表达 | 否 |
| M2 | 加入物理基函数 + 分区 | 基函数标定 | 否 |
| M3 | 多太阳风条件、NN 拟合系数 | 条件模型 | 是 |
| M4 | 无散度 + 外推验证 | 验收报告 | 否 |
| M5 | 导出 ONNX / 纯解析式部署 | 可迁移交付物 | 否 |

---

## 9. 风险与回退方案

> 本节为企划书补充的**空缺**：此前未谈失败判定。

| 风险 | 影响 | 回退方案 |
|---|---|---|
| 磁层顶/弓激波间断拟合差 | 远场精度不达标 | 显式参数化间断面（物理基函数），退回预案 A 分区 |
| 全 NN 外推发散 | 训练集外不可用 | 退回"物理基函数 + NN 系数"，物理形式兜底 |
| gauge 问题导致训练发散 | 无法收敛 | 加库仑规范正则，或改用基函数锁定规范 |
| Ex/Ey/Ez 未写入输出 | 无法直接拟合 E | 切预案 B（E=-∇Φ），或从 `-u×B` 推导 |
| 数据量超出 RAM | IO 成为瓶颈 | 分批预处理 + HDF5/内存映射 |

---

## 10. 资源预算

| 资源 | 需求 |
|---|---|
| 计算 | EPYC 9845（数据生成 + 训练）、RTX 5080（可选，混合精度训练） |
| 内存 | 128 GB（预处理数据驻留 RAM），建议预留 256 GB |
| 存储 | NVMe SSD，原始 SWMF 输出 20–100 GB + 预处理张量 7–34 GB |
| GPU | **无需多卡**，单卡显存 < 2 GB；不建议购置 V100 |

---

## 11. 补充的空缺清单（本次企划书新增）

为便于你确认，列出此前讨论中未覆盖、本次自主补充的内容：

1. **验收标准**（量化精度/性能/无散度指标）
2. **训练/验证/测试划分 + 防泄漏 + 外推测试**
3. **坐标系与单位制规范**（GSE/GSM 选择、SI vs 归一化）
4. **磁矢势 gauge 自由度处理**（库仑规范正则）
5. **磁层顶/弓激波间断的采样加密与间断感知损失**
6. **基函数具体选型**（偶极/Harris/Shue/弓激波）
7. **太阳风参数空间 DOE 采样设计**
8. **稳态 vs 时间序列的明确界定**
9. **与 Tsyganenko 的基准对比**
10. **部署交付形态**（ONNX/纯解析式/C）
11. **失败判定与回退方案**
12. **实验可复现性**（随机种子、数据版本、checkpoint）
13. **物理对称性数据增强**（南北对称、偶极轴对齐）

---

*注：第 2.2 节精度阈值、第 3.4 节基函数具体形式、第 4.2 节采样范围均为初始建议，需在 M0/M1 拿到真实数据后标定。*
